---
category: Fixed
---

- Fixed language backend selection after manifests are added or removed in a warmed directory tree by validating a bounded insertion-ordered manifest-root cache topology.
- Unverifiable directory metadata now stops discovery at the local boundary instead of crossing into an ancestor project, and secondary-language profiles are captured before selector awaits.
