# One PR-review severity vocabulary, a closed gate bypass, and salvage-tolerant CLEAN attestations

## What changed

The `/swarm pr-review` findings artifact spoke a different severity dialect from the verdict rows it projects, and the mismatch had been absorbed by making the field skippable — which silently disabled a mechanical integrity check (issue #2279).

- **One severity vocabulary, end to end.** Findings records now use `INFO | LOW | MEDIUM | HIGH | CRITICAL | NONE`, matching the `[REVIEWED]`/`[CRITIC]` verdict vocabulary they are a projection of. `NONE` — which a `DISPROVED` critic verdict is *required* to carry — was previously unrepresentable, so a reviewer verdict of `NONE` had no legal encoding. The `[CANDIDATE]` row vocabulary is deliberately unchanged and still excludes `NONE`: a discovered candidate asserting "no severity" is a contradiction.
- **The severity-omission bypass is closed.** All four severity comparisons in the artifact gate were guarded by `if (record.severity && …)`, so omitting the field skipped verification against **both** the reviewer and the critic. Worse, the gate actively instructed callers to omit it whenever the two authorities disagreed. Severity is now required on every record and compared unconditionally; an omitted field is a violation that names the value owed (`severity expected "MEDIUM", got (omitted)`).
- **A critic downgrade is now encodable.** For a critic-routed record at `post_critic`, the critic verdict is the single authority — the final word. Reviewer `MEDIUM` + critic `LOW` persists as `severity: "LOW"` and validates. Non-critic-routed records continue to match the reviewer. There is no longer any record for which no severity value passes.
- **A conflicting `[CLEAN]` attestation is salvaged, not fatal.** A `[CLEAN]` row emitted alongside a `[CANDIDATE]` row for the same lane previously returned a top-level parse error even though the independently validated candidate rows were retained — so the agent reading the tool receipt treated the whole lane as failed and re-dispatched it. The parse now succeeds and discloses `clean_attestation_salvaged` with a reason. The attestation is still discredited exactly as hard as before, so **a salvaged `[CLEAN]` never counts as coverage** and a lane with no candidate rows still fails. A `[CLEAN]` that failed for any other reason — degraded or partial source, duplicate attestation, lane mismatch — still hard-errors and is never reported as a salvage. One reporting detail changed: when a same-lane conflict coincides with malformed rows, the artifact is now reported as a salvage rather than as `untrusted-clean-attestation`; the attestation is discredited either way, so no lane gains coverage from the relabel.
- **A header re-emitted as a data row is dropped as salvage.** Literal template text pasted where a finding belonged used to count as a malformed row, which tripped the zero-malformed-rows rule and destroyed an otherwise-valid `[CLEAN]` attestation — sometimes for a different lane. It is now dropped as a `duplicate-header-row-dropped` repair. The surviving header is always the marker-bearing one.
- **Repairs are disclosed on the tool receipt.** `parse_lane_candidates` previously discarded the repair provenance computed during normalization, so a repaired artifact was indistinguishable from a pristine one. The receipt now reports `repair_kinds`, and a salvaged attestation is recorded on the durable delegation ledger as a distinct `clean-attestation-salvaged` recovery rather than disappearing from it.

## Compatibility

The enum change is additive and findings artifacts persisted before this release still load: the read path is deliberately tolerant of a missing `severity`, and required-ness is enforced only at the write boundary.

## Every boundary now has an authority

`post_explorer` records are compared against the `[CANDIDATE]` row they project: the severity must equal what that row declared. The canonical row parser already validated that value against the candidate vocabulary, so it was available one field short of where the gate needed it — the inventory derivation now carries it through as a by-product, leaving the inventory's own contract untouched. When discovery finds nothing at all, the inventory is the mechanically derived `CLEAN-REVIEW` sentinel, which has no candidate row and whose severity is `NONE` — matching the reviewer row it is mandated to carry — so a zero-finding review never has to invent a severity and then change it. The rule is keyed on whether an authority exists, not on the id: `candidate_id` is free text, so a lane may name a real finding `CLEAN-REVIEW`, and such a record is still compared exactly against its own row.

Closes #2279. Closes #2320.
