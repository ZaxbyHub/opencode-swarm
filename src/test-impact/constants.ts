/** Shared cap for any tool that executes a resolved test-file selection. */
export const MAX_SAFE_TEST_FILES = 50;

/** Version marker for the rebuildable impact-cache envelope. */
export const IMPACT_CACHE_VERSION = 2;
