PR-review lanes now receive an executable contract card and a reversible verdict-row codec, so pipes, backslashes, and line breaks no longer corrupt reviewer or critic evidence; legacy unescaped pipes are recovered with an explicit fidelity classification instead of silently dropping the row, and that recovery is retained on the durable lane ledger. Artifact writers report repairable field-level validation errors, reject payloads that downstream bounded readers cannot safely reload, and recover exact trigger-evaluation retries after response loss without rewriting the immutable receipt. Waited collection deadlines terminalize active review lanes with bounded partial salvage even when the host messages client is unavailable, run IDs are reserved durably with bounded collision retries, and PR-feedback continuation accepts the canonical URL-less handoff command while preserving explicit confirmation.
