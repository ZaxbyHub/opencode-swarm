<!-- custom-release-notes:start -->
# Secretscan hard-gate completion and evidence integrity

## Fixed

- Made standalone and file-scoped secretscan accounting distinguish files that were scanned, intentionally skipped, or left incomplete because of size, read failures, containment rejection, deadlines, cleanup failures, or bounded discovery limits. Bounded path-and-reason diagnostics now identify incomplete coverage.
- Failed both `pre_check_batch` and phase preflight closed on incomplete coverage and persisted the same failed verdict for scanner errors, wrapper timeouts, findings, zero coverage, count mismatches, and incomplete scans. `check_gate_status` now distinguishes incomplete coverage from detected secrets.
- Scanned all content in accepted explicit pre-check files up to the existing 512 KiB bound, including lines longer than 10,000 characters, instead of silently ignoring content after 50 KiB. The standalone directory tool retains its prior long-line skip behavior.
- Classified bounded binary prefixes before treating oversized text files as incomplete, so intentionally unscannable binary assets do not block the hard gate while oversized text remains fail-closed.
- Redacted every secret before emitting shared line context, bounded each context, and enforced the final serialized-output byte ceiling.
- Bounded iterative discovery with deadline checks, event-loop yields, fixed directory/candidate limits, deterministic selection, explicit handle cleanup, and incomplete-coverage reporting.
- Prevented explicit-file scans from following parent symlinks or Windows junctions outside the canonical project root.
- Added end-to-end Stripe detector coverage through `pre_check_batch`, complementing the shared-registry fix that previously shipped in PR #1697.

## Migration

No configuration migration is required. Successful scan results require an `incomplete_files` counter and add bounded `incomplete_paths` diagnostics; internal consumers and persisted evidence were updated together.

## Known caveats

Text files larger than 512 KiB remain intentionally outside the bounded scanner, but are now reported as incomplete and fail the hard gate instead of appearing clean. Binary files are classified from a bounded prefix and skipped intentionally.
<!-- custom-release-notes:end -->

## [7.138.1](https://github.com/ZaxbyHub/opencode-swarm/compare/v7.138.0...v7.138.1) (2026-08-10)


### Bug Fixes

* **secretscan:** fail closed on incomplete coverage ([ebd6586](https://github.com/ZaxbyHub/opencode-swarm/commit/ebd65863503c877e7903ac37c7db3f88ec708ed0))